# Import and revision history

Repository initialization: 2026-09-21. Baseline: the preserved WP1A Gate 1A submission, revision 1.1. Its original 88 manifest entries were checked against the original archive before import. All 89 files, including the manifest itself, are represented in this repository. Existing dated change logs, stable IDs, pre-source-recovery, pre-Claude-review and Gate 0 snapshots are preserved. There was no existing Git history in the source workspace; this commit starts version history without fabricating earlier commits.

The latest direct user instruction specifies Gate 0 approved with conditions, WP1A complete for review, Gate 1A awaiting Brad, and WP1B unauthorized. Later local work exists but is outside this requested baseline; it has not been deleted or imported as approved work. This is an explicit infrastructure baseline selection, not a monetary decision or retroactive claim about other local records.

`import-provenance.json` maps every original path and SHA-256 to its repository path and imported SHA-256. Markdown navigation was adjusted for the new folders. Shared links in partial historical snapshots resolve to included supporting records where possible; those snapshots retain their original dated status and do not control current authorization.

The public-copy ZIP preserves original paths. Private chat URLs and one local attachment account path were withheld in seven files; the exact affected files are listed in the provenance record. The unchanged original ZIP remains local and is not committed. The public copy is explicitly not byte-identical to that original. Embedded historical manifests are retained as evidence of the original submission, not as hashes of redacted or reorganized bytes. Use the import provenance and repository validation for the public import.

SRC003's recovered source and the adopted roadmap retain their original bytes. The maintained draft constitution has two navigation links updated for its new folder; its monetary text is unchanged. The public-copy ZIP preserves the draft constitution's original bytes. SRC003 is authoritative by user-confirmed recovery from the primary conversation, not by formal platform export. Privacy redactions do not alter monetary clauses, recommendations or gate status.

Original validation scripts are historical tools for their original layout; they are not repository automation. Run `python tools/validate_repository.py` to validate this import. No external approval is produced by validation.
